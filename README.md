# Умникум Гордеус — чат-бот на Express.js

Учебное Express-приложение с локальной логикой чат-бота «Умникум Гордеус», выполненное по требованиям контрольной работы: базовый сервер, маршруты, middleware, работа с параметрами, телом запроса и статическими файлами. [web:49]

## Возможности

- Чат-бот с уникальной личностью:
  - Имя: **Умникум Гордеус**
  - Возраст: **8153845 секунд** (возраст пересчитывается динамически)
  - Пол: **мужской**, но по вторникам идентифицирует себя как **круассан с малиной 🥐**
- Локальная «умная» логика без внешних API
- Ответы с юмором, рандомным поведением и внутренними состояниями (настроение, уровень энергии)
- Перезапуск диалога кнопкой **«Очистить»**
- Красивый тёмный интерфейс с розово-красными акцентами

## Стек технологий

- Node.js
- Express.js
- Vanilla JavaScript (frontend)
- HTML5, CSS3 (статические файлы через `express.static`) [web:37]

## Структура проекта


.
├── controllers/
│   └── chatController.js
├── middleware/
│   └── logger.js
├── routes/
│   └── chat.js
├── public/
│   ├── index.html
│   ├── style.css
│   └── script.js
├── src/
│   ├── 1scr.png
│   ├── 2scr.png
│   └── 3scr.png
├── server.js
└── package.json


## Установка и запуск


git clone <repo-url>
cd <project-folder>

npm install
npm start


После запуска приложение будет доступно по адресу:  
`http://localhost:3000`

## Описание API

### GET `/api/chat`

Получение последних сообщений чата.

Примеры:

- `GET /api/chat` — вернуть последние 50 сообщений по умолчанию  
- `GET /api/chat?limit=10` — вернуть последние 10 сообщений

Пример ответа:


{
  "success": true,
  "messages": [
    {
      "id": 1,
      "user": "bot",
      "text": "Пример сообщения бота",
      "timestamp": "2025-12-09T09:00:00.000Z"
    }
  ],
  "umnikumStatus": {
    "age": "8 153 845",
    "mood": "playful",
    "energy": 100,
    "tuesday": true
  }
}


### POST `/api/chat`

Отправка сообщения чат-боту.

Тело запроса:


{
  "message": "Привет, кто ты?"
}


Пример ответа:


{
  "success": true,
  "messages": [
    { "id": 123, "user": "user", "text": "Привет, кто ты?", "timestamp": "..." },
    { "id": 124, "user": "bot", "text": "Ответ Умникума Гордеуса", "timestamp": "..." }
  ]
}


### POST `/api/chat/reset`

Перезапуск диалога.  
Очищает историю и добавляет новое стартовое приветствие Умникума.

Пример ответа:


{
  "success": true,
  "messages": [
    {
      "id": 1,
      "user": "bot",
      "text": "🔄 Диалог перезапущен! ...",
      "timestamp": "..."
    }
  ]
}


## Скриншоты

Главный экран чата с темной темой и Умникумом Гордеусом:

![Главный экран чата](./src/1scr.png)

Пример диалога с ботом и различными ответами:

![Диалог с ботом](./src/2scr.png)

Кнопка «Очистить» и перезапуск диалога:

![Функция выписывания времени](./src/3scr.png)

## Соответствие требованиям контрольной

- Базовый Express-сервер (`server.js`)
- Маршруты:
  - `GET /api/chat`
  - `POST /api/chat`
  - `POST /api/chat/reset`
- Параметры:
  - `req.query.limit` в `GET /api/chat`
- Обработка тела запроса:
  - `express.json()` и `express.urlencoded()` в `server.js` [web:42]
- Собственный middleware:
  - `middleware/logger.js` — логирование запросов и добавление `req.timestamp`
- Раздача статики:
  - `express.static('public')` — `index.html`, `style.css`, `script.js` [web:37]
- Разнесение логики по модулям:
  - `routes/chat.js`
  - `controllers/chatController.js`
- Локальная логика чат-бота:
  - Класс `UmnikumGordeys` с состояниями, паттернами и генерацией ответов
